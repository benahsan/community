package community

class BootStrap {

    def init = { servletContext ->
        // def family = new Family(headOfFamily: 'أحمد فوزان بن أحسن عزام',  
        //     contact: new Contact(whatsappNumber: '٦٠١٢٣٤٥٦٧٨٩', mobileNumber: '٧٧٧٦٦٦٥٥٥'), 
        //     fileNumber: '١٢٣٤').save()

        // new Member(name: 'أحمد فوزان بن أحسن عزام', headOfFamily: family).save()
        // new Member(name: 'محمد بن أحمد فوزان', headOfFamily: family).save()
        // new Member(name: 'فاطمة الزهراء بنت أحمد فوزان', headOfFamily: family).save()
    }
    def destroy = {
    }
}
